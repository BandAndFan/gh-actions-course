name: 01 BUilding Blocks

on: push

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
        - name: Say hello
          run: echo "Hello, World!"
  echo-goodbye:
      runs-on: ubuntu-latest
      steps:
        - name: Successful step
          run: | # this vertical line is required when running multi-line scripts
            echo "I will succeed"
            exit 0
        - name: Say goodbye
          run: echo "Goodbye!"
          # This step will not be executed
              